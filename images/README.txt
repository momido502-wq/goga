Replace or add memories using these simple filenames:

photo1.jpg through photo10.jpg

The gallery automatically uses the paths listed in the loveStory configuration object in ../script.js.
Missing images become elegant “Future memory” cards instead of broken image icons.
